<!-- user_can( int|WP_User $user, string $capability, mixed $args ): bool -->

<!-- Use AJAX to communicate between JS and PHP -->
function id_subject()
{
    if (user_can($user->ID, 'edit_posts'))
}
